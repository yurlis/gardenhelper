<?php $siteUrl = $_SERVER['DOCUMENT_ROOT'];include $siteUrl . '/modules/config/db.php';include $siteUrl . '/modules/config/random.php';$data = "";// if (isset($_GET['u_code'])) {// 	$sql = "SELECT * FROM users WHERE confirm_mail ='". $_GET['u_code'] ."' ";// 	$result = $conn->query($sql);// 	if ($result->num_rows > 0) {// 		$u_verifed = mysqli_fetch_assoc($result);// 		$sql = "UPDATE `users` SET `verifed` = '1' WHERE `users`.`id` = '". $u_verifed['id'] ."' ";// 		// if ($conn->query($sql)) {// 		// 	$data = [// 		// 		'status' => true,// 		// 		'message' => "Пользователь верифицирован!"// 		// 	];// 		// } // 	}// }if (isset($_POST) and $_SERVER["REQUEST_METHOD"] == "POST") {	if (isset($_POST['name']) && isset($_POST['email']) && $_POST['name'] != "" && $_POST['email'] != "" && isset($_POST['phone']) && $_POST['phone'] != "") {		if ($_POST['repassword'] != $_POST['password']) {			$data = [				'status' => false,				'message' => "Passwords is not equal"			];		} else {			// === проверка есть ли такой телефон в базе			$sql = "SELECT * FROM `users` WHERE `phone` LIKE '". $_POST['phone'] ."' ";			$result = $conn->query($sql);			if ( $result->num_rows > 0 ) {				$data = [					'status' => false,					'message' => "This phone already used. Choose another"				];			} else {				// === проверка есть ли такой е-майл в базе				$sql = "SELECT * FROM `users` WHERE `email` LIKE '". $_POST['email'] ."' ";				$result = $conn->query($sql);				if ($result->num_rows > 0) {					$data = [						'status' => false,						'message' => "Email is busy. Use another"					];				} else {					// === добавление пользователя					//случайный набор символов					$u_code = random_str();					//Кодировка пароля					$password = md5($_POST['repassword']);					$sql = "INSERT INTO users (login, password, email, phone, confirm_mail) VALUES ('". $_POST['name'] ."', '". $password ."', '". $_POST['email'] ."', '". $_POST['phone'] ."', '$u_code')";					if ($resultat = $conn->query($sql)) {						$link = "<a href='http://gardenhelper.local/modules/user/send_verification.php?u_code=$u_code'>Подтвердить</a>";						mail($_POST['email'], 'Подтверждение електронной почты', $link);						$data = [							'status' => false,							'id' => $conn->insert_id,							'message' => "Вам на почту отправлено письмо. При переходе по ссылке вы подтвердите свою почту"						];					}				} // === / добавление пользователя 			} // === / емейла в базе нет		} // === / пароли совпадают	} // === / не все поля введены	else {		$data = [			'status' => false,			'message' => "All fields must be filled"		];	}} // === / пришел запрос POSTecho json_encode($data);?>